<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Express Skills</title>
</head>
<body>
    <h1>Mad Skill - Show</h1>

    <h2>Skill #<%= skill.id %></h2>
    <!-- skill. is the variable is defined in controllers show() function -->
    <h3><%= skill.skill %></h3>
    <h3>Complete: <%= skill.accomplished ? 'Yes' : 'No' %></h3>


    <!-- 'method=' is the CRUD verb and 
        'action=' is the endpoint request (where the new delete will render localhost:3000/skills/new) -->
    
        <form action="/skills/<%= skill.id %>?_method=DELETE" class="delete-form" method="POST">
            <button type="submit">Delete Skill</button>
           
          </form>
</body>
</html>